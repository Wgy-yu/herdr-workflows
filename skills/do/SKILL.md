---
name: do
description: 启动 Herdr 多 Agent 开发实施闭环。用户要求由 Leader、实施者和审核者执行开发、返修和最终验收时使用。
---

# Herdr do

1. 完整读取并遵循 `../herdr-workflows/SKILL.md`。
2. 固定使用 `mode=do`，不得切换到其他 mode。
3. 将用户随本 Skill 提供的其余文本作为运行时参数。
4. 本入口只声明 mode 和参数；不复制或改写核心 Skill 的规则。
