---
name: config
description: 配置本机 Herdr Agent 能力、启动参数和默认分工。用户要求设置 Agent、模型、权限参数或全局角色时使用。
---

# Herdr config

1. 完整读取并遵循 `../herdr-workflows/SKILL.md`。
2. 固定使用 `mode=config`，不得切换到其他 mode。
3. 将用户随本 Skill 提供的其余文本作为运行时参数。
4. 本入口只声明 mode 和参数；不复制或改写核心 Skill 的规则。
