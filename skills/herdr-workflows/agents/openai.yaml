interface:
  display_name: "Herdr Workflows"
  short_description: "初始化、配置并运行可移植的 Herdr 多智能体工作流"
  default_prompt: "Use $herdr-workflows to initialize, configure, review, or execute a Herdr multi-agent workflow."
policy:
  allow_implicit_invocation: true
