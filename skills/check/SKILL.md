---
name: check
description: 检查本机 Herdr Agent、启动适配器与 Superpowers 状态。用户要求检查 Herdr 环境、Agent 安装或可用性时使用。
---

# Herdr check

1. 完整读取并遵循 `../herdr-workflows/SKILL.md`。
2. 固定使用 `mode=check`，不得切换到其他 mode。
3. 将用户随本 Skill 提供的其余文本作为运行时参数。
4. 本入口只声明 mode 和参数；不复制或改写核心 Skill 的规则。
